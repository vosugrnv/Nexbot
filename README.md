# Nexbot

Nhân bản từ dự án [VuaProxy](https://github.com/vosugrnv/VuaProxy).

## Local

- Frontend: static HTML/CSS/JS (có thể deploy Vercel)
- API: `api/` (Node)

```bash
cd api && npm install && npm start
```

File `.env` / `.env.local` không commit — copy từ `api/.env.example` nếu cần.
